## 🎹 Shortcuts Guide

### 🖥 MacOS
- `esc` — **Close app**
- `⌘ (Left CMD)` + `⌘ (Right CMD)` — **Pause/Unpause**
- `s` — **Show stats** (only when paused)
- `t` — **Open `text.txt`** (only when paused)
